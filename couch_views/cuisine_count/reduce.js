function(key,values,rereduce) {
  //var retval=undefined;
  //if(!rereduce) {
    //retval=[key[0][0],sum(values)];
  //}
  //else {
    //var k=values[0][0];
    //var sum2=0;
    //for(var kv in values) {
      //sum2 = sum2 + values[kv][1];
    //}
    //retval= [k,sum2];
  //}
  //return retval;
  return sum(values);
}
